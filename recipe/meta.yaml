{% set name = "pykalman" %}
{% set version = "0.11.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pykalman-{{ version }}.tar.gz
  sha256: 3cf7cd8f5c96d51e3285aaef12eb2818fe872e092c3d649f1e4033c42639b806

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >61
    - pip
  run:
    - python >={{ python_min }}
    - numpy <3
    - packaging
    - scikit-base <0.14.0
    - scipy <2.0.0

test:
  imports:
    - pykalman
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pykalman.github.io
  license: BSD-3-Clause
  license_file: LICENSE
  license_family: BSD
  summary: 'An implementation of the Kalman Filter, Kalman Smoother, and EM algorithm in Python'
  dev_url: https://github.com/pykalman/pykalman
  doc_url: https://pykalman.github.io

extra:
  recipe-maintainers:
    - fkiraly
    - pmlandwehr
    - ocefpaf
